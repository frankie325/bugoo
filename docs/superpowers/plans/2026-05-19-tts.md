# 本地系统 TTS API 实施计划

> **给 agentic workers：** REQUIRED SUB-SKILL: 使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 按任务逐步实施本计划。步骤使用 checkbox（`- [ ]`）语法跟踪。

**目标：** 打通 API-only 的本地系统 TTS 链路：前端 `speakText(text, lang?)` 调用现有 Tauri `speak_text` command。

**架构：** Tauri command 层保持轻量，只负责接收 IPC 参数并委托给后端 TTS 模块。平台相关的系统命令选择与进程启动放在 `src-tauri/src/tts/mod.rs`；该模块提供可测试的纯 helper 来处理文本归一化和命令构造，单元测试不需要真正播放音频。前端沿用现有 `src/lib/api/*.ts` invoke helper 模式，不新增 UI。

**技术栈：** Tauri 2 IPC、Rust `std::process::Command`、React/Vite TypeScript API helper、`AGENTS.md` 中定义的现有项目构建命令。

---

## 文件结构

| 文件 | 职责 |
| --- | --- |
| `src-tauri/src/tts/mod.rs` | 归一化输入、选择平台命令、启动本地系统 TTS。 |
| `src-tauri/src/commands/tts.rs` | Tauri command wrapper，委托给 `crate::tts::speak_text`。 |
| `src/lib/api/tts.ts` | 前端 invoke helper：`speakText(text, lang?)`。 |
| `src/lib/api/index.ts` | 将 TTS helper 和其它 API helper 一起重新导出。 |

本计划不修改 UI、设置、数据库、`audio_url` 或 HeroUI 文件。

---

### Task 1: 实现可测试的 Rust TTS 核心

**Files:**
- Modify: `src-tauri/src/tts/mod.rs`

- [ ] **Step 1: 写入文本 trim、空文本和平台命令构造的单元测试与最小实现**

将 `src-tauri/src/tts/mod.rs` 替换为以下内容：

```rust
use std::process::Command;

pub fn speak_text(text: &str, lang: Option<&str>) -> Result<(), String> {
    let Some(text) = normalize_text(text) else {
        return Ok(());
    };

    let mut command = build_system_tts_command(&text, lang);
    command
        .spawn()
        .map_err(|error| format!("failed to start system TTS: {error}"))?;

    Ok(())
}

fn normalize_text(text: &str) -> Option<String> {
    let trimmed = text.trim();
    if trimmed.is_empty() {
        None
    } else {
        Some(trimmed.to_owned())
    }
}

fn build_system_tts_command(text: &str, lang: Option<&str>) -> Command {
    platform_tts_command(text, lang)
}

#[cfg(target_os = "macos")]
fn platform_tts_command(text: &str, lang: Option<&str>) -> Command {
    let mut command = Command::new("say");

    if is_chinese_lang(lang) {
        command.args(["-v", "Ting-Ting"]);
    }

    command.arg(text);
    command
}

#[cfg(target_os = "windows")]
fn platform_tts_command(text: &str, _lang: Option<&str>) -> Command {
    let mut command = Command::new("powershell");
    command.args([
        "-NoProfile",
        "-Command",
        &format!(
            "Add-Type -AssemblyName System.Speech; \
             $s = New-Object System.Speech.Synthesis.SpeechSynthesizer; \
             $s.Speak({});",
            powershell_single_quoted(text)
        ),
    ]);
    command
}

#[cfg(target_os = "linux")]
fn platform_tts_command(text: &str, _lang: Option<&str>) -> Command {
    let mut command = Command::new("spd-say");
    command.arg(text);
    command
}

#[cfg(not(any(target_os = "macos", target_os = "windows", target_os = "linux")))]
fn platform_tts_command(_text: &str, _lang: Option<&str>) -> Command {
    Command::new("__bugoo_unsupported_tts_platform__")
}

#[cfg(target_os = "macos")]
fn is_chinese_lang(lang: Option<&str>) -> bool {
    lang.map(|value| {
        let normalized = value.to_ascii_lowercase();
        normalized == "zh" || normalized.starts_with("zh-") || normalized.starts_with("zh_")
    })
    .unwrap_or(false)
}

#[cfg(target_os = "windows")]
fn powershell_single_quoted(value: &str) -> String {
    format!("'{}'", value.replace('\'', "''"))
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn normalize_text_returns_none_for_blank_input() {
        assert_eq!(normalize_text(""), None);
        assert_eq!(normalize_text("   \n\t  "), None);
    }

    #[test]
    fn normalize_text_trims_non_blank_input() {
        assert_eq!(normalize_text("  hello  "), Some("hello".to_owned()));
    }

    #[cfg(target_os = "macos")]
    #[test]
    fn macos_command_uses_say_with_text() {
        let command = build_system_tts_command("hello", Some("en"));

        assert_eq!(command.get_program().to_string_lossy(), "say");
        let args: Vec<String> = command
            .get_args()
            .map(|arg| arg.to_string_lossy().into_owned())
            .collect();
        assert_eq!(args, vec!["hello"]);
    }

    #[cfg(target_os = "macos")]
    #[test]
    fn macos_command_uses_chinese_voice_for_chinese_lang_hint() {
        let command = build_system_tts_command("你好", Some("zh-CN"));

        let args: Vec<String> = command
            .get_args()
            .map(|arg| arg.to_string_lossy().into_owned())
            .collect();
        assert_eq!(args, vec!["-v", "Ting-Ting", "你好"]);
    }

    #[cfg(target_os = "windows")]
    #[test]
    fn windows_command_uses_powershell_system_speech() {
        let command = build_system_tts_command("Bob's word", Some("en"));

        assert_eq!(command.get_program().to_string_lossy(), "powershell");
        let args: Vec<String> = command
            .get_args()
            .map(|arg| arg.to_string_lossy().into_owned())
            .collect();
        assert_eq!(args[0], "-NoProfile");
        assert_eq!(args[1], "-Command");
        assert!(args[2].contains("System.Speech.Synthesis.SpeechSynthesizer"));
        assert!(args[2].contains("'Bob''s word'"));
    }

    #[cfg(target_os = "linux")]
    #[test]
    fn linux_command_uses_spd_say_with_text() {
        let command = build_system_tts_command("hello", Some("en"));

        assert_eq!(command.get_program().to_string_lossy(), "spd-say");
        let args: Vec<String> = command
            .get_args()
            .map(|arg| arg.to_string_lossy().into_owned())
            .collect();
        assert_eq!(args, vec!["hello"]);
    }
}
```

- [ ] **Step 2: 运行 Rust 测试，确认核心行为正确**

运行：

```bash
cd src-tauri && cargo test tts
```

预期：PASS。这个版本已经包含能让测试通过的最小实现；测试重点是纯 helper 和命令构造，不需要真的播放音频。

- [ ] **Step 3: 检查核心实现没有超出范围**

确认 `src-tauri/src/tts/mod.rs` 没有引入外部 crate、没有写文件、没有更新 `audio_url`，也没有等待子进程播放完成。

- [ ] **Step 4: 提交 Rust 核心**

```bash
git add src-tauri/src/tts/mod.rs
git commit -m "feat: implement local system tts core"
```

---

### Task 2: 接入 Tauri Command

**Files:**
- Modify: `src-tauri/src/commands/tts.rs`

- [ ] **Step 1: 将空 command 替换为委托 wrapper**

将 `src-tauri/src/commands/tts.rs` 替换为：

```rust
#[tauri::command]
pub fn speak_text(text: String, lang: Option<String>) -> Result<(), String> {
    crate::tts::speak_text(&text, lang.as_deref())
}
```

- [ ] **Step 2: 运行后端构建，确认 command 签名可用**

运行：

```bash
cd src-tauri && cargo build
```

预期：PASS。`src-tauri/src/lib.rs` 中的 `tauri::generate_handler![commands::tts::speak_text]` 接受更新后的 command 签名，注册列表不需要变更。

- [ ] **Step 3: 运行后端测试**

运行：

```bash
cd src-tauri && cargo test
```

预期：PASS。TTS 单元测试和已有后端测试都通过。

- [ ] **Step 4: 提交 command 接入**

```bash
git add src-tauri/src/commands/tts.rs
git commit -m "feat: wire tts tauri command"
```

---

### Task 3: 新增前端 TTS API Helper

**Files:**
- Create: `src/lib/api/tts.ts`
- Modify: `src/lib/api/index.ts`

- [ ] **Step 1: 创建前端 helper**

创建 `src/lib/api/tts.ts`：

```ts
import { invoke } from "@tauri-apps/api/core";

export async function speakText(
  text: string,
  lang?: string,
): Promise<void> {
  return invoke("speak_text", {
    text,
    lang: lang ?? null,
  });
}
```

- [ ] **Step 2: 从 API barrel 导出 helper**

在 `src/lib/api/index.ts` 末尾追加：

```ts
export { speakText } from "./tts";
```

编辑后完整文件应为：

```ts
export { translate, type TranslationResult } from "./translate";
export {
  addWord,
  getWords,
  deleteWord,
  updateWord,
  type Word,
  type WordUpdate,
} from "./word";
export { getSettings, setSetting, seedSettings } from "./settings";
export {
  getTags,
  createTag,
  updateTag,
  deleteTag,
  reorderTags,
} from "./tags";
export { speakText } from "./tts";
```

- [ ] **Step 3: 运行前端构建**

运行：

```bash
npm run build
```

预期：PASS。TypeScript 接受 `src/lib/api/tts.ts`，Vite 构建通过。如果当前环境没有 `npm`，使用可用的等价项目构建命令或 bundled Node runtime，并在实施记录中写明实际 fallback 命令。

- [ ] **Step 4: 提交前端 API helper**

```bash
git add src/lib/api/tts.ts src/lib/api/index.ts
git commit -m "feat: add frontend tts api helper"
```

---

### Task 4: 最终验证

**Files:**
- Verify only; no planned source edits.

- [ ] **Step 1: 运行前端构建**

运行：

```bash
npm run build
```

预期：PASS。如果 Vite 输出 chunk size warning，但命令退出码为 0，可以接受。

- [ ] **Step 2: 运行 Tauri 后端构建**

运行：

```bash
cd src-tauri && cargo build
```

预期：PASS。

- [ ] **Step 3: 运行后端测试**

运行：

```bash
cd src-tauri && cargo test
```

预期：PASS。

- [ ] **Step 4: 确认实现范围**

运行：

```bash
git diff --name-only HEAD
```

预期：如果每个任务都已经提交，没有输出。如果有刻意保留的未提交改动，输出只能包含：

```text
src-tauri/src/tts/mod.rs
src-tauri/src/commands/tts.rs
src/lib/api/tts.ts
src/lib/api/index.ts
```

同时确认实现没有修改 `src/pages/`、`src/stores/`、`src/locales/` 或数据库模块。

- [ ] **Step 5: 可选手动音频冒烟测试**

如果本地正在运行完整 Tauri app，可以临时从开发专用 console 路径或本地 scratch UI 调用导出的 helper：

```ts
import { speakText } from "./lib/api";

await speakText("hello", "en");
await speakText("你好", "zh-CN");
```

预期：每次调用都会启动系统语音。测试后删除所有临时代码，不提交 UI 或 scratch 文件。

- [ ] **Step 6: 记录最终实施结果**

记录哪些验证命令通过。如果跳过了手动音频冒烟测试，写明原因，例如：

```text
Manual audio smoke test skipped because implementation was verified in non-interactive CI-like shell.
```

---

## 自查

- Spec 覆盖：计划实现 `speakText(text, lang?)`，接入 `speak_text`，只使用本地系统命令，`spawn` 后返回，并避免 UI、设置、数据库、音频缓存和云端 TTS。
- 占位符扫描：没有保留占位实现步骤；每个源码编辑步骤都提供了完整代码。
- 类型一致性：前端发送 `lang: string | null`，Tauri 接收 `Option<String>`，Rust 核心接收 `Option<&str>`。
